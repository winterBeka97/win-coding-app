<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="windows_app_coding.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:windows_app_coding"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="windows-app-coding"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="windows-app-coding">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/light-dodo-trans.ico" />
            </TitleBar.IconSource>
            <TitleBar.Content>
                <TextBlock Text="Cododo" FontSize="16" FontWeight="Bold" />
            </TitleBar.Content>
        </TitleBar>

        <!--
            The Frame hosts pages for your application content. Add your UI to
            MainPage.xaml rather than here so you can use Page features such as
            navigation events and the Loaded lifecycle.
        -->
        <Frame x:Name="RootFrame" Grid.Row="1" />
    </Grid>
</Window>
